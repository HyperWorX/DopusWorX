# Typography and lists showcase

Switch between Reading and Live: the two should match.

Body text now sits a small step in from the heading's left edge (a gentle
hanging-heading look), while list bullets sit further in still. This paragraph
is the reference: notice its left edge is slightly right of the "Typography and
lists showcase" heading above, in both modes.

## Lists

- First bullet item
- Second bullet item
- Third bullet item
  - A nested item, indented one more step
  - Another nested item
- Back to the top level

1. Numbered one
2. Numbered two
3. Numbered three

The vertical gap between list items should read the same in Reading and Live.

## A short paragraph between headings

Another paragraph so you can see the inset applied consistently. Quotes and code
blocks keep their own left edge (they have their own frame), so only running
prose takes the inset.

> A blockquote keeps its left border at the card edge; only the prose paragraphs
> are inset.

```js
// Code blocks are not inset either - they have their own panel.
const x = 1;
```

Closing paragraph.
